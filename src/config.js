// --- API CONFIGURATION ---

// In a real Vite project, you would use: import.meta.env.VITE_API_URL
// However, to ensure this code runs in the preview environment without errors, 
// we will default to the localhost string here.

// When deploying to GoDaddy:
// 1. Ensure your .env file has VITE_API_URL=https://your-backend-domain.com/api
// 2. You can uncomment the line below if your build environment supports it:
// export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';

export  const API_URL ='https://me-life-coaching-production-server.vercel.app/api';
 //export  const API_URL='https://me-life-coaching-production-server-phi.vercel.app/api';